import React from 'react';

// This component is intentionally left empty to remove the conflicting
// react-three-fiber code that was causing the application to crash.
export const Scene: React.FC = () => {
  return null;
};
